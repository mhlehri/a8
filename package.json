{
  "name": "assignment-8",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx watch src/server.ts",
    "build": "prisma generate && tsc --outDir dist",
    "start": "node dist/server.js",
    "prod": "NODE_ENV=production node ./dist/server.js",
    "postinstall": "prisma generate",
    "vercel-build": "prisma generate && prisma migrate deploy && tsc --outDir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.1",
  "dependencies": {
    "@prisma/client": "6.9.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "zod": "^3.25.55"
  },
  "devDependencies": {
    "@types/cors": "^2.8.18",
    "@types/express": "^5.0.2",
    "prisma": "^6.9.0",
    "@types/node": "^22.15.30",
    "tsx": "^4.19.4"
  }
}
